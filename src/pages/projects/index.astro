---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import ProjectFilters from '../../components/ProjectFilters.astro';
import SectionHeading from '../../components/SectionHeading.astro';
import { secondaryWork } from '../../data/site';
import { sortedProjects as projects } from '../../data/projects';
import { withBase, isExternal } from '../../utils/paths';

const allTags = [...new Set(projects.flatMap((p) => p.tags ?? []))].sort();
---

<BaseLayout
  title="Selected Work"
  description="Projects and selected work by Reese Hollister across research, teaching, product work, and public scholarship."
>
  <section class="page-hero site-shell">
    <p class="section-kicker">Portfolio</p>
    <h1>Selected Work</h1>
    <p class="page-lead">
      Concrete examples of student support, educational product development, research organization, and public communication — followed by a broader archive of North Africa research, fieldwork, and Arabic study.
    </p>
  </section>

  <section class="site-shell">
    <SectionHeading
      eyebrow="Selected Work"
      title="Case studies and supporting projects"
      intro="Start with the four leading case studies, or filter the full collection by topic."
    />
    <ProjectFilters categories={allTags} />
    <div class="project-grid">
      {projects.map((project) => <ProjectCard project={project} framed />)}
    </div>
  </section>

  <section class="site-shell">
    <SectionHeading
      eyebrow="Elsewhere"
      title="Public work and supporting links"
      intro="Other parts of the public record that support the projects here."
    />
    <div class="support-grid">
      {secondaryWork.map((item) => (
        <article class="support-card">
          <p class="section-kicker">{item.category}</p>
          <h3>{item.title}</h3>
          <p>{item.summary}</p>
          <div class="action-list">
            <a
              class="resource-link"
              href={withBase(item.href)}
              target={isExternal(item.href) ? '_blank' : undefined}
              rel={isExternal(item.href) ? 'noreferrer' : undefined}
            >
              {item.cta}
            </a>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
